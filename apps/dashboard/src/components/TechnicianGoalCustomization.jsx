import { useEffect, useMemo, useState } from "react";
import { fetchGoals, saveGoals } from "../api/managementApi";
const LABELS={revenue:"Revenue",closingRate:"Closing %",billableServiceCalls:"Billable Calls",installRevenue:"Install Revenue",installAverageTicket:"Average Ticket",membershipsSold:"Memberships",opportunities:"Opportunities"};
function effective(result,techId,id){const own=result.technicianGoals?.[String(techId)]?.[id];return own??result.goals?.[id]??"";}
export function TechnicianGoalCustomization(){const[state,setState]=useState({loading:true,data:null,period:"today",techId:"",company:{},tech:{},status:"",error:false,confirm:null});
const load=async(period=state.period)=>{setState(s=>({...s,loading:true,status:""}));try{const result=await fetchGoals(period);const techId=state.techId||String(result.technicians?.[0]?.id||"");const company={};for(const id of result.editableGoalIds||[]){const vals=(result.technicians||[]).map(t=>effective(result,t.id,id));company[id]=vals.length&&vals.every(v=>String(v)===String(vals[0]))?vals[0]:"";}const tech=Object.fromEntries((result.editableGoalIds||[]).map(id=>[id,effective(result,techId,id)]));setState(s=>({...s,loading:false,data:result,period,techId,company,tech,status:"",error:false,confirm:null}));}catch{setState(s=>({...s,loading:false,error:true,status:"Could not load goals."}));}};
useEffect(()=>{load("today");},[]);const ids=state.data?.editableGoalIds||[];const techs=state.data?.technicians||[];const labels=state.data?.periodLabels||{};const selected=useMemo(()=>techs.find(t=>String(t.id)===state.techId),[techs,state.techId]);
if(state.loading&&!state.data)return <div className="operations-alert">Loading goals…</div>;
const chooseTech=(techId)=>setState(s=>({...s,techId,tech:Object.fromEntries(ids.map(id=>[id,effective(s.data,techId,id)])),status:""}));
const saveTech=async()=>{const values={};for(const id of ids){const raw=state.tech[id];values[id]=raw===""?null:Number(raw);}try{await saveGoals({period:state.period,technicians:{[state.techId]:values}});await load(state.period);}catch(e){setState(s=>({...s,error:true,status:e.message}));}};
const requestCompany=()=>{const changed=ids.filter(id=>state.company[id]!=="");if(!changed.length)return;const differences=[];for(const id of changed){const target=Number(state.company[id]);const current=techs.map(t=>({name:t.name,value:effective(state.data,t.id,id)}));if(current.some(x=>Number(x.value)!==target))differences.push({id,target,current});}if(differences.length)setState(s=>({...s,confirm:differences}));else applyCompany(changed);};
const applyCompany=async(changed=ids.filter(id=>state.company[id]!==""))=>{const technicians={};for(const t of techs)technicians[String(t.id)]=Object.fromEntries(changed.map(id=>[id,Number(state.company[id])]));try{await saveGoals({period:state.period,technicians});await load(state.period);}catch(e){setState(s=>({...s,confirm:null,error:true,status:e.message}));}};
return <section className="technician-goal-customization"><div className="settings-title"><span className="mobile-eyebrow">Independent targets</span><h2>Goals</h2><p>Goals are stored separately for every reporting period. Company changes apply to every technician; individual changes affect only the selected technician.</p></div>
<label className="goal-editor-select"><span>Goal period</span><select value={state.period} onChange={e=>load(e.target.value)}>{(state.data?.periods||[]).map(p=><option key={p} value={p}>{labels[p]||p}</option>)}</select></label>
<div className="goal-editor-panel"><h3>Company Goals</h3><p>If technicians currently differ, the field is blank. Entering a value sets that goal for everyone after confirmation.</p><div className="technician-goal-grid">{ids.map(id=><label key={id}><span>{LABELS[id]}</span><input type="number" min="0" value={state.company[id]??""} placeholder="Mixed technician goals" onChange={e=>setState(s=>({...s,company:{...s.company,[id]:e.target.value}}))}/></label>)}</div><button className="operations-primary" type="button" onClick={requestCompany}>Apply Company Goals</button></div>
<div className="goal-editor-panel"><h3>Technician Goals</h3><label className="goal-editor-select"><span>Technician</span><select value={state.techId} onChange={e=>chooseTech(e.target.value)}>{techs.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><p>Editing {selected?.name||"this technician"} does not change anyone else.</p><div className="technician-goal-grid">{ids.map(id=><label key={id}><span>{LABELS[id]}</span><input type="number" min="0" value={state.tech[id]??""} onChange={e=>setState(s=>({...s,tech:{...s.tech,[id]:e.target.value}}))}/></label>)}</div><button className="operations-primary" type="button" onClick={saveTech}>Save {selected?.shortName||selected?.name||"Technician"} Goals</button></div>
{state.confirm&&<div className="goal-confirm-overlay" role="dialog" aria-modal="true"><div className="goal-confirm"><h3>Change everyone's goals?</h3><p>These technicians do not all have the new values. Confirming will overwrite each listed current goal for {labels[state.period]||state.period}.</p>{state.confirm.map(d=><div className="goal-confirm__metric" key={d.id}><strong>{LABELS[d.id]} → {d.target}</strong>{d.current.map(x=><span key={x.name}>{x.name}: {x.value===""?"Not set":x.value}</span>)}</div>)}<div className="goal-management__actions"><button onClick={()=>setState(s=>({...s,confirm:null}))}>Cancel</button><button className="operations-primary" onClick={()=>applyCompany()}>Yes, change everyone</button></div></div></div>}
{state.status&&<div className={`operations-alert${state.error?" is-error":" is-success"}`}>{state.status}</div>}</section>;}
