import { fetchDashboardPeriod, setDashboardPeriod } from "./api/managementApi";
const OPTIONS=[["today","Today"],["yesterday","Yesterday"],["wtd","Week to Date"],["last-week","Last Week"],["mtd","Month to Date"],["last-month","Last Month"],["qtd","Quarter to Date"],["last-quarter","Last Quarter"],["ytd","Year to Date"],["last-year","Last Year"],["last-7-days","Last 7 Days"],["last-30-days","Last 30 Days"]];
const LABELS=Object.fromEntries(OPTIONS);
function install(){if(window.location.pathname!=="/remote")return;const card=document.querySelector(".dashboard-period-card");if(!card||card.querySelector(".dashboard-period-select"))return;const toggle=card.querySelector(".dashboard-period-toggle");if(!toggle)return;toggle.style.display="none";const label=document.createElement("label");label.className="dashboard-period-select";label.innerHTML=`<span>Select range</span><select aria-label="Select reporting period">${OPTIONS.map(([value,text])=>`<option value="${value}">${text}</option>`).join("")}</select>`;toggle.insertAdjacentElement("afterend",label);const select=label.querySelector("select");const title=card.querySelector("div:first-child strong");fetchDashboardPeriod().then(({period})=>{select.value=period;select.dataset.current=period;if(title)title.textContent=LABELS[period]||period;}).catch(()=>{});select.addEventListener("change",async()=>{const previous=select.dataset.current||"today";select.disabled=true;try{const result=await setDashboardPeriod(select.value);select.dataset.current=result.period;select.value=result.period;if(title)title.textContent=LABELS[result.period]||result.period;window.location.reload();}catch{select.value=previous;select.disabled=false;}});}
const observer=new MutationObserver(install);const start=()=>{install();observer.observe(document.body,{childList:true,subtree:true});};if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();
