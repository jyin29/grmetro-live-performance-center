"use strict";
const packageJson=require("../package.json");const{createApp}=require("./app");const{loadConfig}=require("./config");const{createLogger}=require("./logger");const{createRefreshProvider}=require("./providers/createRefreshProvider");const{DashboardCache}=require("./cache/dashboardCache");const{RefreshScheduler}=require("./refresh/refreshScheduler");const{installGracefulShutdown}=require("./server");const{TvManager}=require("./tv/tvManager");const{ExpirationMonitor}=require("./tv/expirationMonitor");const{createBrowserManagerForConfig}=require("./browser/createBrowserManager");const{createServiceTitanClient}=require("./servicetitan/createServiceTitanClient");const{PRESENTATION_DISPLAYS,PRESENTATION_SLIDE_COUNT,PRESENTATION_ROTATION_MILLISECONDS}=require("../../../shared/presentation");const{createPresentationManager}=require("./presentation/presentationManager");const{createPresentationCommandBus}=require("./presentation/presentationCommandBus");const{createPresentationWebSocket}=require("./presentation/presentationWebSocket");const{createDisplayPresence}=require("./routes/presentationRoutes");const{createEventEngine}=require("./events/eventEngine");const{GoalStore}=require("./goals/goalStore");const{DisplaySettingsStore}=require("./settings/displaySettingsStore");const{SpreadsheetSlideStore}=require("./settings/spreadsheetSlideStore");
function start(){const startedAt=new Date();const config=loadConfig();const logger=createLogger({level:config.logLevel});const browserManager=createBrowserManagerForConfig(config,logger);const serviceTitanClient=browserManager?createServiceTitanClient({config,browserManager,logger}):null;const goalStore=new GoalStore();const displaySettingsStore=new DisplaySettingsStore();const spreadsheetSlideStore=new SpreadsheetSlideStore();let scheduler;const provider=createRefreshProvider({config,browserManager,executor:serviceTitanClient?.executor,logger,goalsProvider:()=>goalStore.getGoals(scheduler?.getPeriod?.()||"today")});const cache=new DashboardCache({snapshotRetentionLimit:config.snapshotRetentionLimit,trendMinimumHistory:config.trendMinimumHistory});const tvManager=new TvManager({overrideMilliseconds:config.remoteOverrideSeconds*1000,returnTransitionMilliseconds:config.returnTransitionMilliseconds});const expirationMonitor=new ExpirationMonitor({tvManager});const eventEngine=createEventEngine();const presentationManager=createPresentationManager({displays:PRESENTATION_DISPLAYS,slideCount:PRESENTATION_SLIDE_COUNT,rotationMilliseconds:PRESENTATION_ROTATION_MILLISECONDS,eventEngine});const presentationCommandBus=createPresentationCommandBus({handleCommand:presentationManager.handleCommand});const displayPresence=createDisplayPresence();let presentationWebSocket;scheduler=provider?new RefreshScheduler({provider,cache,logger,intervalMilliseconds:config.refreshIntervalSeconds*1000,timeZone:config.timeZone,onSuccessfulPayload:(payload)=>{eventEngine.process(payload);presentationWebSocket?.broadcastDashboardUpdate(payload);}}):null;const mockBrowserStatus=()=>({connected:false,connecting:false,serviceTitanPageFound:false,inactiveReason:"mock-mode",lastConnectedAt:null,lastDisconnectedAt:null,reconnectAttempt:0,lastErrorCode:null,lastErrorMessage:null});const browserStatusProvider=browserManager?()=>browserManager.getStatus():mockBrowserStatus;const serviceTitanStatusProvider=serviceTitanClient?()=>serviceTitanClient.getStatus():()=>({status:"bypassed"});const connectionStatusProvider=()=>{const ws=presentationWebSocket?.getConnectionSummary()||{total:0,displays:0,remotes:0,byDisplay:{}};const byDisplay={...ws.byDisplay};for(const id of displayPresence.getOnlineDisplayIds()){const current=byDisplay[id]||{displays:0,remotes:0,total:0};byDisplay[id]={...current,displays:Math.max(1,current.displays||0),total:Math.max(1,current.total||0)};}const displays=Object.values(byDisplay).filter(x=>(x.displays||0)>0).length;return{...ws,displays,byDisplay};};const app=createApp({config,logger,cache,tvManager,scheduler,applicationVersion:packageJson.version,buildVersion:process.env.BUILD_VERSION||null,browserStatusProvider,serviceTitanStatusProvider,serviceTitanClient,goalStore,displaySettingsStore,spreadsheetSlideStore,presentationRuntime:{presentationManager,commandBus:presentationCommandBus,displayPresence},adminRuntime:{presentationManager,eventEngine,startedAt,connectionStatusProvider,scheduler,browserStatusProvider,serviceTitanStatusProvider}});const server=app.listen(config.port,config.host,()=>logger.info("Backend application started",{application:packageJson.name,version:packageJson.version,nodeEnv:config.nodeEnv,host:config.host,port:config.port,refreshProvider:config.mockMode?"mock":"servicetitan"}));presentationWebSocket=createPresentationWebSocket({server,manager:presentationManager,commandBus:presentationCommandBus,logger,displayPresence});installGracefulShutdown({server,logger,scheduler:{stop(){scheduler?.stop();expirationMonitor.stop();presentationWebSocket.close();presentationManager.destroy();eventEngine.destroy();serviceTitanClient?.stop();browserManager?.stop();}}});expirationMonitor.start();browserManager?.start();scheduler?.start();return{server,cache,scheduler,tvManager,expirationMonitor,browserManager,serviceTitanClient,presentationManager,presentationWebSocket,eventEngine,displaySettingsStore,spreadsheetSlideStore};}
if(require.main===module)start();module.exports={start};
