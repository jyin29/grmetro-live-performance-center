"use strict";
const express=require("express");const businessRules=require("../../../../shared/businessRules");const slides=require("../../../../shared/slides");const{PRESENTATION_DISPLAYS,PRESENTATION_ROTATION_MILLISECONDS}=require("../../../../shared/presentation");
function createAdminRoutes({cache,presentationManager,connectionStatusProvider,eventEngine,applicationVersion,buildVersion=null,startedAt=new Date(),clock=()=>new Date()}={}){if(!cache?.getState||!presentationManager?.getDisplayStates)throw new TypeError("Admin routes require cache and presentation runtime state.");const router=express.Router();router.get("/",(request,response)=>{const now=clock();const cacheState=cache.getState(now);const connections=connectionStatusProvider?.()||{total:0,displays:0,remotes:0,byDisplay:{}};const eventState=eventEngine?.getState?.()||{activeEvent:null,queueLength:0};const hasData=cacheState.available===true;const hasRefreshError=Boolean(cacheState.lastFailure?.code);const displays=presentationManager.getDisplayStates().map(display=>{const clients=connections.byDisplay?.[display.displayId]||{displays:0,remotes:0,total:0};const online=(clients.displays||0)>0;return{...display,connectedClients:{displays:clients.displays||0,remotes:clients.remotes||0,total:clients.displays||0,socketTotal:clients.total||0},displayOnline:online,status:online?"connected":"offline",currentSlide:slides[display.activeSlideIndex]||null};});response.set("Cache-Control","no-store, no-cache, must-revalidate").json({generatedAt:now.toISOString(),displays,businessRules:{rules:businessRules.rules,settings:businessRules.settings},events:{activeEvent:eventState.activeEvent,pendingEvents:eventState.queueLength,queueSize:businessRules.settings.maximumQueueSize,cooldownMilliseconds:businessRules.settings.cooldownMilliseconds,eventDurationMilliseconds:businessRules.settings.eventDurationMilliseconds,overlayDurationMilliseconds:businessRules.settings.overlayDurationMilliseconds},presentation:{rotationIntervalMilliseconds:PRESENTATION_ROTATION_MILLISECONDS,profiles:[...new Set(PRESENTATION_DISPLAYS.map(({presentationProfile})=>presentationProfile))],slides:slides.map(({id,label,durationSeconds})=>({id,label,durationSeconds}))},diagnostics:{enabledByDefault:false,cacheAvailable:hasData,cacheAgeMilliseconds:cacheState.cacheAgeMilliseconds,lastSuccessfulRefreshAt:cacheState.lastSuccessfulRefreshAt,lastFailureCode:cacheState.lastFailure?.code||null},system:{applicationVersion,buildVersion,uptimeSeconds:Math.max(0,Math.floor((now-startedAt)/1000)),websocketStatus:"running",backendStatus:"healthy",dashboardStatus:hasRefreshError?"error":(hasData?"available":"waiting-for-data"),connectedClients:connections.total||0,connectedDisplays:connections.displays||0,connectedRemotes:connections.remotes||0}});});return router;}
module.exports={createAdminRoutes};
